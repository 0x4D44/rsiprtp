# 2026-04-30 — Overnight Fuzz Campaign

**Operator:** Claude (supervisor)
**Window:** 2026-04-30 22:00 BST → 2026-05-01 06:00 BST (8 hours)
**Goal:** Run a multi-target cargo-fuzz campaign across the rsiprtp workspace, triage any crashes, and produce fixes/PRs for what we find.

## Plan of attack

1. **Inventory fuzz targets** (Explore agent) — rank by attack-surface value.
2. **Bootstrap a `fuzz/` crate** under the workspace (no existing infra). Use libfuzzer-sys via `cargo fuzz` (nightly toolchain is already installed).
3. **Seed corpora** from existing unit tests / sample data where possible.
4. **Run targets in parallel as background processes**, time-sliced so each gets meaningful CPU.
5. **Watch for crashes** via Monitor on each fuzzer's output; on crash, dispatch a triage agent (investigate → diagnose → fix → review).
6. **Journal regularly** (every meaningful event + ~hourly heartbeat).

## Targets (initial guess, to be confirmed by Explore agent)

- `rsiprtp-rtp`: RTP packet parse, RTCP compound parse, DTMF telephone-event parse
- `rsiprtp-sdp`: SDP body parse
- `rsiprtp-sip`: any of our own wrappers around `rsip` (skip rsip itself)
- `rsiprtp-ice`: STUN message parse
- `rsiprtp-srtp`: SRTP unprotect (auth tag must be plausible)
- `rsiprtp-transport`: framing if any

## Environment

- Stable + nightly toolchains present.
- `cargo-fuzz 0.13.1` installed at `/home/md/.cargo/bin/cargo-fuzz`.
- No existing `fuzz/` crate anywhere under the workspace.
- Workspace already builds clean per `e01fc0b`.

## Status log

- **22:00** — Plan drafted, journal opened, target inventory dispatched.
- **22:30** — Bootstrapped `fuzz/` standalone crate (12 bins). All build under `cargo +nightly fuzz build`. Seeded corpora (1-3 inputs per target) with hand-crafted minimal valid RTP/RTCP/SDP/SIP/STUN/SRTP messages.
- **22:35** — Launched all 12 fuzzers in parallel as background processes, `-max_total_time=21600` (6h) each, `-timeout=10`, `-rss_limit_mb=2048`.
- **22:36** — **Crash #1 found: `rtcp_sr`** after 13 inputs. Panic in `bytes-1.11.1/src/lib.rs:170` — `advance out of bounds: the len is 3 but advancing by 4`. Crash bytes saved to `fuzz/triage/rtcp_sr-001/crash.bin` (27 bytes; `[80 c8 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 12 01 00 00 0a 05 00 80]`). Other 11 targets running healthy at 25k–700k exec/sec.
- **22:36** — Dispatching crash-triage agent.
- **22:38** — **Crash #1 fixed.** Root cause: `SenderReport::parse` (`rtcp.rs:258`) checked `rest.len() < 20` before reading 24 bytes of sender info (SSRC + 2×NTP + RTP-ts + pkt-count + octet-count). 27-byte input → 23 bytes after header → slipped past, panicked in `Buf::advance(4)`. Fix: `rtcp.rs:259` bumped check to `< 24`. Regression test `test_sr_parse_truncated_fuzz_001` added. `cargo test -p rsiprtp-rtp` 229/229 passing, clippy clean. Triage agent surveyed peer parsers (RR/Nack/Pli/Fir/Remb) and reported their own length checks are sufficient. Restarted `rtcp_sr` against patched code at 22:38 (5h50m budget remaining). _No commit; will batch with later fixes._
- **22:38** — **Crash #2 found: `sdp_session`** after ~3M iterations. Panic at `parser.rs:55:30` — `start byte index 2 is not a char boundary; it is inside '\u{9e2a3}'`. Classic UTF-8 boundary bug from `&str` byte-index slicing. Crash bytes (41b) saved to `fuzz/triage/sdp_session-001/crash.bin`. Other 10 targets still running healthy. Dispatching triage agent.
- **22:40** — **Crash #2 fixed.** Root cause: `parser.rs:55` did `&line[2..]` after `chars().nth(1) == Some('=')` — that check counts CHARS not BYTES, so a 4-byte first codepoint slips past it and the byte-index slice splits mid-codepoint. Fix: replaced char-based guard with byte-based `bytes[0].is_ascii_alphabetic() && bytes[1] == b'='` (RFC 4566 only allows ASCII-letter type codes anyway). Regression test `test_parse_non_ascii_first_char_fuzz_001` added. `cargo test -p rsiprtp-sdp` 217/217 passing, clippy clean. Agent confirmed (after grep sweep) this was the only `&line[..N]`-style byte slice in the SDP parser. Restarted `sdp_session` against patched code at 22:40.

### Setup: heartbeat monitor

- Crash detection: relying on bg-task failure notifications (libfuzzer exits non-zero on first crash → bg task fails → I get a notification).
- Journaling cadence: 30-min heartbeat monitor emits a status line; on each tick I sweep all logs and journal.
- _Heartbeat timestamps are system-clock (UTC); my early entries used user-local "22:xx". From here forward I'll use system clock so events line up with notifications._

### Heartbeat 1 — 07:23 system time (≈30 min into campaign)

All 12 targets healthy, no new crashes since the two early fixes:

| target          |     iterations | exec/s  | corp | cov  |
|-----------------|---------------:|--------:|-----:|-----:|
| dtmf_event      | 1,073,741,824 | 458 k   |    4 |   26 |
| rtcp_fir        |   536,870,912 | 247 k   |   30 |   73 |
| rtcp_header     |   536,870,912 | 410 k   |    5 |   36 |
| rtcp_nack       |   268,435,456 | 152 k   |   27 |   77 |
| rtcp_pli        |   536,870,912 | 362 k   |    8 |   42 |
| rtcp_remb       |   536,870,912 | 316 k   |   22 |   72 |
| rtcp_rr         |   536,870,912 | 225 k   |   16 |   64 |
| rtcp_sr (run2)  |   268,435,456 | 216 k   |   23 |   72 |
| rtp_packet      |   536,870,912 | 308 k   |   23 |  106 |
| sdp_session(r2) |    44,639,450 |  22 k   | 1144 |  756 |
| sip_message     |    32,537,072 |  13 k   | 3132 | 2124 |
| srtp_use_ext    | 1,073,741,824 | 839 k   |    1 |   14 |

Aggregate: ≈3.6 M exec/sec. `srtp_use_ext` saturated coverage early (small parser, only 14 features) — it's mostly hammering null inputs at this point but since we're parallel we leave it. `sip_message` and `sdp_session` are the deepest parsers and have huge corpora; they're the most likely to keep finding new bugs in subsequent hours.

No new artifacts beyond the two already triaged. Fixes still uncommitted (waiting for end-of-campaign batch).

### Heartbeat 2 — 07:55 system time (≈1h into campaign)

No new crashes. Two deep parsers still discovering edges — strong reason to keep them running:
- `sip_message`: cov 2124 → 2342 (+218), corp 3132 → 3725 (+593), 32M → 58M iter
- `sdp_session`: cov 743 → 756 (+13), corp 1144 → 1169, 44M → 91M iter

All others have flat-lined coverage (expected — small/narrow parsers). exec/sec stable across the board. Memory still well within the 2 GB ceiling per process. ≈5 h of fuzz time still budgeted; will let run.

### Heartbeat 3 — 08:28 system time (≈1h33m in)

No new crashes. Deltas vs HB2:
- `sip_message`: cov 2342 → 2464 (+122), corp 4074, 85M iter
- `sdp_session`: cov 756 (flat), ft 4169 → 4204, corp 1178, 138M iter
- All others: coverage saturated; fuzzing on plateau (still useful — checks invariants on all the structured mutations).

### Heartbeat 4 — 09:01 system time (≈2h05m in)

No new crashes. `sip_message` cov 2464 → 2542 (+78). `sdp_session` ft 4204 → 4215 (+11), cov flat at 756. Everything else stable.

### Heartbeat 5 — 09:33 system time (≈2h40m in)

No new crashes. `sip_message` cov 2542 → 2590 (+48), 130M iter, 4435 corp. `sdp_session` 244M iter, cov 756 still. Plateau on the saturated targets.

### Heartbeat 6 — 10:06 system time (≈3h13m in)

No new crashes. `sip_message` cov +20 to 2610. `sdp_session` cov 756 (flat). Coverage growth across all targets has substantially flattened — diminishing returns territory. Will keep all targets running through the remaining ≈3h to maximize the chance of catching deeper cases (and to keep the fuzzers' mutation pressure on edge cases of the already-discovered edges).

### Heartbeat 7 — 10:39 system time (≈3h45m in)

No new crashes. `sip_message` cov +20 to 2630, 179M iter, 4570 corp. `sdp_session` cov 756 still, ft +3 to 4227. Saturation continuing.

### Heartbeat 8 — 11:12 system time (≈4h18m in, past midpoint)

No new crashes. `sip_message` cov +19 to 2649. `sdp_session` truly flat — same cov, same ft, same corp size as last tick. ≈2h45m of fuzz time left on the budget.

### Heartbeat 9 — 11:45 system time (≈4h51m in)

No new crashes. `sip_message` cov +2 to 2651 — barely moving. `sdp_session` ft +2 but cov unchanged. Saturation deep across all 12 original targets.

**Decision:** Strong saturation across original 12 targets. Adding 5 new targets to spend the last ~3h on undiscovered surface.

### Round 2 targets launched — 11:50 system time

| target          | crate         | function                              |
|-----------------|---------------|---------------------------------------|
| `sip_uri`       | rsiprtp-sip   | `SipUri::parse(&str)`                 |
| `sip_via`       | rsiprtp-sip   | `Via::parse(&str)`                    |
| `sip_digest`    | rsiprtp-sip   | `DigestChallenge::parse(&str)`        |
| `ice_candidate` | rsiprtp-ice   | `Candidate::from_sdp(&str)`           |
| `srtp_sdes`     | rsiprtp-srtp  | `SdesAttribute::parse(&str)`          |

All 5 built green, seeded with 2-3 valid examples each. Time budget: `-max_total_time=10000` (≈2h47m, ends ~14:35).

- **11:51** — **Crash #3 found: `srtp_sdes`** after only ≈310 k iterations. Panic at `sdes.rs:147:29` — `attempt to shift left with overflow`. Crashing input has a `2^292` lifetime token; the parser is doing `1u64 << exponent` without checking the exponent bound. Crash bytes saved to `fuzz/triage/srtp_sdes-001/crash.bin`. Other 4 round-2 targets running. Dispatching triage agent.
- **11:57** — **Crash #3 fixed.** Root cause: `parse_key_params` at `sdes.rs:147` did `1u64 << exp` without bound-checking `exp` (a parsed `u32`) — `2^292` triggers the shift overflow. Fix: bound `exp <= 48` per RFC 4568 §6.1 (SRTP lifetime SHALL NOT exceed 2^48 for supported AES-CM suites); returns the existing `String` error variant. Regression test `test_sdes_parse_huge_lifetime_fuzz_001` covers `2^292`, `2^64`, `2^49`, plus sanity-passing `2^31` and `2^48`. `cargo test -p rsiprtp-srtp` 120/120, clippy clean. Restarted `srtp_sdes` at 11:58.

### Heartbeat 10 — 12:18 system time (≈5h25m in / round-2 ≈28 min)

All 17 fuzzers healthy, no new crashes. Round-2 progress:
- `sip_digest`: 34M iter, cov 428, ft 1933, 662 corp — best coverage signal
- `srtp_sdes` (run2): 67M iter, cov 551 (up from 486 at restart), 492 corp — patched code is exploring more
- `sip_via`: 88M iter, 47k exec/s — fastest mutation rate of round-2
- `sip_uri`: 64M iter, cov 295, 409 corp
- `ice_candidate`: 73M iter, cov 229, 287 corp

Originals all flat. `sip_message` cov +3 to 2654.

- **12:34** — `sdp_session` (run2) completed its full 5h45m budget. **520,802,853 iterations, 25 k exec/s, no crashes** after the round-1 fix. Strong validation of the parser fix; SDP parser is otherwise robust to malformed input within reach of libfuzzer mutation pressure starting from one valid seed.
- **12:43** — `rtcp_sr` (run2) completed its 5h50m budget. **4,353,709,384 iterations, 207 k exec/s, no crashes** after the round-1 fix. Strong validation.
- **12:43** — All 12 originals have now reached their time budget. Final stats:

| target          |        iter | exec/s   | cov | ft   |
|-----------------|------------:|---------:|----:|-----:|
| dtmf_event      | 10,093 M    | 467 k    |  26 |   27 |
| rtcp_fir        |  4,790 M    | 222 k    |  73 |  166 |
| rtcp_header     |  8,098 M    | 375 k    |  36 |   37 |
| rtcp_nack       |  3,378 M    | 156 k    |  77 |  175 |
| rtcp_pli        |  8,082 M    | 374 k    |  42 |   43 |
| rtcp_remb       |  6,756 M    | 313 k    |  72 |  108 |
| rtcp_rr         |  4,786 M    | 222 k    |  64 |  140 |
| rtcp_sr (run2)  |  4,354 M    | 207 k    |  72 |  148 |
| rtp_packet      |  6,872 M    | 318 k    | 106 |  139 |
| sdp_session(r2) |    521 M    |  25 k    | 756 | 4230 |
| sip_message     |    251 M    |  12 k    |2665 |12458 |
| srtp_use_ext    | 17,483 M    | 809 k    |  14 |   15 |
| **TOTAL**       | **75,463 M**|          |     |      |

≈75.5 billion iterations across the 12 originals over their 6h budget. Two crashes found+fixed; no further crashes after fix. Round-2 still has ~1h45m to run.

### Heartbeat 11 — 12:51 system time (round-2 ≈1h in)

Round-2 healthy, no new crashes. Coverage growth has flattened across all 5 round-2 targets (cov deltas: sip_uri 0, sip_via +1, sip_digest +5, ice_candidate 0, srtp_sdes 0). All five clearly past their saturation knee. Will let them finish out their budget.

### Heartbeat 12 — 13:24 system time (round-2 ≈1h35m in)

No new crashes. Tiny cov deltas (sip_via +1, sip_digest +2, ice_candidate +1, srtp_sdes +1, sip_uri 0). exec/sec rising across the board (sip_via now 76 k/s, ice_candidate 63 k/s, srtp_sdes 70 k/s) — round-2 targets are picking up freed CPU from the originals.

### Heartbeat 13 — 13:57 system time (round-2 ≈2h7m in, ~38 min left)

No new crashes. Cov deltas tiny: ice_candidate +1, all others 0. Growth essentially stopped. exec/sec stable at higher post-original level. Will let the budget complete.

- **14:28** — `srtp_sdes` (run2) completed its 2h30m budget. **740,707,132 iterations, 82 k exec/s, no crashes** after the round-2 shift-overflow fix. Strong validation.
- **14:34** — `sip_uri`, `sip_via`, `sip_digest`, `ice_candidate` all completed cleanly (each ≈10 000 s budget). No crashes.
- **14:34** — Heartbeat monitor stopped. Campaign closed.

## Final report

### Targets, iterations, and findings

**Round 1 (originals, 6 h budget each):**

| target          |        iter | exec/s | cov  |   ft  | result                                     |
|-----------------|------------:|-------:|-----:|------:|--------------------------------------------|
| dtmf_event      | 10,093 M    | 467 k  |   26 |    27 | clean                                       |
| rtcp_fir        |  4,790 M    | 222 k  |   73 |   166 | clean                                       |
| rtcp_header     |  8,098 M    | 375 k  |   36 |    37 | clean                                       |
| rtcp_nack       |  3,378 M    | 156 k  |   77 |   175 | clean                                       |
| rtcp_pli        |  8,082 M    | 374 k  |   42 |    43 | clean                                       |
| rtcp_remb       |  6,756 M    | 313 k  |   72 |   108 | clean                                       |
| rtcp_rr         |  4,786 M    | 222 k  |   64 |   140 | clean                                       |
| **rtcp_sr** (run2) |  4,354 M  | 207 k  |   72 |   148 | **crash #1 → fix → 4.35 B clean**         |
| rtp_packet      |  6,872 M    | 318 k  |  106 |   139 | clean                                       |
| **sdp_session**(run2) |  521 M | 25 k  |  756 |  4230 | **crash #2 → fix → 521 M clean**           |
| sip_message     |    251 M    |  12 k  | 2665 | 12458 | clean                                       |
| srtp_use_ext    | 17,483 M    | 809 k  |   14 |    15 | clean                                       |

**Round 2 (added at hr 5, 2h47m budget each):**

| target          |     iter | exec/s | cov | ft   | result                                     |
|-----------------|---------:|-------:|----:|-----:|--------------------------------------------|
| sip_uri         |    565 M | 56 k   | 295 | 1081 | clean                                       |
| sip_via         |    870 M | 87 k   | 286 | 1130 | clean                                       |
| sip_digest      |    318 M | 32 k   | 439 | 2019 | clean                                       |
| ice_candidate   |    732 M | 73 k   | 231 |  651 | clean                                       |
| **srtp_sdes** (run2) |  741 M | 82 k | 552 | 1857 | **crash #3 → fix → 741 M clean**           |

**Grand total:** ≈78.7 billion fuzz iterations across 17 targets in five crates (rsiprtp-rtp, rsiprtp-sip, rsiprtp-sdp, rsiprtp-ice, rsiprtp-srtp). Three crashes found, all fixed and locked down with regression tests; restarted fuzzers ran their full post-fix budgets clean.

### Crashes and fixes

| # | target        | site                                  | bug                                                                 | fix                                                  | regression test                          |
|---|---------------|---------------------------------------|---------------------------------------------------------------------|------------------------------------------------------|------------------------------------------|
| 1 | rtcp_sr       | `crates/rsiprtp-rtp/src/rtcp.rs:258`  | `SenderReport::parse` checked `< 20` before reading 24 sender-info bytes → `Buf::advance(4)` panic on truncated input | bumped check to `< 24`                               | `test_sr_parse_truncated_fuzz_001`       |
| 2 | sdp_session   | `crates/rsiprtp-sdp/src/parser.rs:55` | `chars().nth(1) == Some('=')` then `&line[2..]` — char-count vs byte-index split mid-codepoint on multi-byte first char → UTF-8 boundary panic | byte-level guard `bytes[0].is_ascii_alphabetic() && bytes[1] == b'='`                | `test_parse_non_ascii_first_char_fuzz_001` |
| 3 | srtp_sdes     | `crates/rsiprtp-srtp/src/sdes.rs:147` | `parse_key_params` did `1u64 << exp` on user-supplied `2^N` lifetime token without bounding `exp` → shift-left overflow | bound `exp <= 48` per RFC 4568 §6.1                  | `test_sdes_parse_huge_lifetime_fuzz_001` |

All three are local panics on hostile input — no memory unsafety, no auth bypass, no info leak — but in a network-facing telephony stack each is a remotely triggerable DoS, so worth fixing.

### Code touched

- `crates/rsiprtp-rtp/src/rtcp.rs` — +18 −1 (one-line bound-check + 17-line regression test)
- `crates/rsiprtp-sdp/src/parser.rs` — +24 −2 (~8 lines of byte-level type-prefix check + regression test)
- `crates/rsiprtp-srtp/src/sdes.rs` — +46 −0 (5-line bound check + 41-line regression test covering several over-the-limit values plus boundary)
- `cargo test --workspace` confirmed each crate's full test suite still passes; clippy clean.

### Infrastructure left in place

The campaign produced a reusable `fuzz/` crate at the workspace root (libfuzzer-sys, standalone — not a workspace member). 17 fuzz targets, seeded corpora, `.gitignore` for `target/`, `corpus/`, `artifacts/`, and `coverage/`. Three crash artifacts saved under `fuzz/triage/<target>-NNN/crash.bin` alongside the run log that produced them — historical record so the regression tests have provenance even after corpus cleanup. None of this is committed; that's deferred to the user.

### Time accounting

| phase                          | wall-clock |
|--------------------------------|-----------:|
| Setup (inventory + harnesses + seeds + smoke build) | ≈30 min |
| Round-1 fuzzing                | 6 h        |
| Round-2 fuzzing (overlapping last ≈3 h of round-1)  | 2h47m   |
| Triage + fix + restart (3 crashes)                   | ≈15 min   |
| **Total**                      | ≈8 h       |

### Recommendations

- **Commit** `fuzz/` (sans `corpus/` / `artifacts/` / `target/`) plus the three small parser fixes — three small, focused commits.
- **CI**: add a short `cargo +nightly fuzz run <target> -- -max_total_time=120` per push to catch low-hanging-fruit regressions on these parsers (cheap; the targets all execute at 100 k+/s so 2 min is millions of iterations).
- **Future fuzz work** worth investing time in:
  - Stateful fuzzing of `RtpSession` / `RtcpCompound` / dialog state machines (much higher ceiling than per-message parsers; requires a structured-input harness).
  - SRTP `unprotect()` with a known key — fuzz the post-decryption packet path.
  - `rsip` is a third-party crate, so its parsing wasn't fuzzed here; if upstream doesn't have its own fuzz, it would be worth contributing.

