# Coverage Progress and Plan

Date: 2026.01.13

## Current Coverage Status (latest successful llvm-cov run)
- Source: `cargo +nightly llvm-cov --branch --summary-only --workspace --exclude gabby --ignore-run-fail -- --test-threads=1` (post-resolver refactor).
- Regions: 98.57%
- Lines: 99.07%
- Functions: 98.58%
- Branches: 100.00%

Note: Branch and line/region/function targets remain above 98%. llvm-cov still reports "mismatched data" for 33 functions, but branch misses are now 0 across the workspace. Full lcov report generated at `target/llvm-cov/coverage.lcov` and shows 0 missing branches. A full run without `--exclude gabby` still fails to link `gabby` on Windows (missing `libvosk.lib`).

## Recent Progress (since last report)
- Refactored ICE `get_local_addresses` to use function pointers for deterministic branch coverage, added success-path unit test, and updated error-path tests.
- Expanded SDP negotiation tests to cover fmtp attributes, encoding-mismatch no-match, non-audio answers, and invalid formats.
- Reworked transport resolver helpers to use boxed futures (single instantiation) and adjusted resolver tests accordingly; added SRV error fallback in address fallback test.
- Added coverage-only `inline(never)` attributes on transaction manager hot paths and allowed `cfg(coverage)` in crate lints to avoid warnings during llvm-cov runs.
- Added a targeted cleanup test for INVITE server termination to cover the remaining branch in `transaction/manager.rs`.
- Coverage now at 100% branch with 0 misses in the summary report.

## Largest Branch Coverage Gaps (from latest summary)
- None below 98% branch; all summary branches are at 100%.

## Multi-Stage Plan to Reach >98% Branch Coverage

### Stage 0: Stabilize Coverage Runs (Baseline)
Goal: Repeatable branch coverage runs on Windows without timeouts or locked binaries.
- Run `cargo +nightly llvm-cov --branch --workspace --exclude gabby --summary-only -- --test-threads=1` after clearing any hung test processes.
- If timeouts persist, run per-crate branch coverage (ICE/transport/media/transaction) and aggregate hotspots before the full run.
- Confirm no leftover `mdsiprtp_*` test executables are running after coverage runs.
Exit criteria: Full workspace branch report generated twice in a row with no failures.

### Stage 1: Branch-Heavy Core Paths (Media + Transport + Transaction)
Goal: Lift the largest branch gaps with deterministic unit tests.
- Media jitter: cover remaining pop/expand/accelerate/empty-buffer decisions and adapt_delay edge thresholds.
- TLS/TCP: cover send on missing connection, parse_content_length edge cases, read-loop close paths, and error branches.
- Transaction manager: cover missing transaction cases, timer paths, and response handling branches.
Exit criteria: Each of these files reaches >= 90% branch coverage.

### Stage 2: ICE Agent and TURN/STUN
Goal: Cover remaining STUN/TURN state transitions without external servers.
- ICE agent: add deterministic tests for state transitions, nomination, timeout/refresh branches, and invalid message handling.
- TURN: add tests for allocate/refresh errors, permission/nonce handling, and channel binding edge cases.
Exit criteria: `agent.rs` >= 90% branch coverage; `turn.rs` >= 92%.

### Stage 3: SIP Dialog/Session/Resolver Cleanup
Goal: Close remaining branch gaps in dialog/session/transport resolver modules.
- Add tests for dialog and session manager edge cases (unknown handles, missing headers, non-standard Via parsing).
- Expand resolver coverage for NAPTR/SRV/address lookup fallbacks and IPv6 bracket edge cases.
Exit criteria: dialog/session/resolver files >= 95% branch coverage.

### Stage 4: Global Sweep to >98%
Goal: Push remaining deltas and verify totals.
- Re-run llvm-cov after each stage; track branch deltas in a short log.
- Add missing micro-tests for any files still below 98% (SDP, RTP/RTCP, misc utilities).
Exit criteria: Overall branch coverage > 98%, plus line/region/function coverage >= 98% on two consecutive runs.
Status: Met on 2026.01.13 (summary-only run shows branch 100.00%, line/region/function >= 98%).
