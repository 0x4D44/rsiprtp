# Coverage Progress and Plan (2026.01.14)

## Current Coverage Snapshot (llvm-cov --branch)
- Regions: 99.36%
- Functions: 99.09%
- Lines: 99.84%
- Branches: 100%

## Progress Highlights
- Added targeted tests across RTP, SDP, transport, session, and transaction layers to cover parsing and error paths.
- Added failing-writer coverage tests for SDP and SIP URI display paths; made SRTP KDF derivation infallible where errors were unreachable.
- Introduced test-only error injection for TCP/TLS accept loops and UDP recv errors.
- Reduced branch gaps to 0% missed branches; remaining misses are region/line/function partials.
- Confirmed llvm-cov JSON report generated at `target/llvm-cov/coverage.json` for precise gap mapping.

## Current Gaps (Top Offenders)
- `crates/mdsiprtp-ice/src/turn.rs`: error-path coverage for socket bind/connect/send/recv plus assert branches.
- `crates/mdsiprtp-ice/src/agent.rs` and `crates/mdsiprtp-ice/src/stun.rs`: STUN/TURN request error branches.
- `crates/mdsiprtp-rtp/src/rtcp.rs`: parse error branches for malformed RTCP packets.
- `crates/mdsiprtp-transport/src/tls.rs`: read-loop spawn for existing connections and TLS write/error paths.
- `crates/mdsiprtp-sdp/src/builder.rs`: Display formatting error paths (fmt::Write failure not exercised).

## Multi-Stage Plan to Reach 100%
### Stage 1 - Fully Unexecuted Lines
- Force `SessionDescription::fmt` error paths with a failing `fmt::Write` adapter to cover Display error branches.
- Ensure TLS read loop for existing connections runs by synchronizing on a signal from the spawned task.

### Stage 2 - Error-Path Coverage for Networking
- Add test-only hooks for UDP/TCP/TLS send or bind errors where `?` short-circuit paths are uncovered.
- Introduce deterministic failure modes for TURN/STUN send/recv (similar to existing forced accept error).
- Add targeted tests that validate error mapping (`TransportError`, `TurnError`, `StunError`).

### Stage 3 - Parser/Codec/Message Edge Cases
- Add unit tests to exercise remaining RTCP/RTP parse error branches.
- Cover SIP header/message parsing error variants in `message.rs`, `headers.rs`, `uri.rs`, and `auth.rs`.
- Exercise SDP builder/parser optional fields and invalid tokens to close remaining regions.

### Stage 4 - Consolidation and Validation
- Re-run llvm-cov (summary + JSON) and eliminate residual gaps.
- Evaluate any genuinely unreachable branches; consider minimal refactors or safe coverage exclusions if needed.
- Update progress journal and report with final numbers.
