# HLD — Remove DTLS-SRTP stub

**Date:** 2026-05-02
**Author:** Arthur + Claude
**Status:** Proposed
**Related:** `wrk_docs/2026.05.01 - Brief - DTLS-SRTP decision.md`

> **Doc convention.** The unsuffixed `2026.05.02 - HLD - Remove DTLS-SRTP stub.md`
> is the canonical, working version. `… - V1.md`, `… - V2.md` are frozen review
> snapshots. Implementer should follow the unsuffixed file.

## Goal

Delete `crates/rsiprtp/src/srtp/dtls.rs` (990 lines, no working handshake)
and every reference to it. After this lands, `rsiprtp::srtp` advertises
only what it does: SDES key exchange (RFC 4568) feeding `SrtpContext`.

## Decision

**Option A (full removal), not A.5.** No internal caller uses any
`srtp::dtls` type, so keeping `Fingerprint` / `SrtpProfile` for
hypothetical external consumers is exactly the kind of premature
abstraction CLAUDE.md warns against. Re-adding them later from git
history is trivial.

## Versioning — bump to **0.3.0**, not 0.2.1

Cargo's pre-1.0 SemVer rule treats `0.x.y → 0.x.(y+1)` as **automatically
compatible**: anyone with `rsiprtp = "0.2"` would silently absorb the
removal on the next `cargo update` and hit a compile error. The brief
flagged this risk explicitly ("anyone who pinned 0.2 expecting DTLS
support") — V1 of this HLD waved it away; V2 corrects it. Pre-1.0 still
has rules — the **minor** is the breaking-change axis.

Workspace version lives in `d:\language\rsiprtp\Cargo.toml:15`
(`[workspace.package]`); the per-crate manifest is `version.workspace =
true` and is *not* the file to edit.

## File-by-file changes

| # | File | Change |
|---|---|---|
| 1 | `crates/rsiprtp/src/srtp/dtls.rs` | **Delete** (all 990 lines incl. its `#[cfg(test)] mod tests`). |
| 2 | `crates/rsiprtp/src/srtp/mod.rs` | Drop `pub mod dtls;` and the `pub use dtls::{…};` re-export block. Module-level doc is already SDES-only. |
| 3 | `crates/rsiprtp/src/lib.rs` | (a) Lines 8-11: rewrite the parenthetical to `"SRTP key exchange uses SDES (RFC 4568); WebRTC-style DTLS-SRTP is out of scope."`. (b) Lines 44-45: replace bullet with `"- [\`srtp\`]: SRTP encryption with SDES key exchange (RFC 4568)"`. |
| 4 | `README.md` | (a) Replace the WebRTC scope bullet (line 135) with `"**WebRTC interop.** No DTLS-SRTP. SRTP is reachable via SDES key exchange (RFC 4568), which works against SIP carriers but not against browsers."`. (b) Update the install snippet (~line 63) `rsiprtp = "0.2"` → `"0.3"`. |
| 5 | `ARCHITECTURE.md` | (a) Line 43 Mermaid label `SRTP & DTLS framing` → `SRTP-SDES`. (b) Lines 91-93: drop the `"plus DTLS-SRTP framing types (the DTLS handshake itself is not yet implemented)"` tail. |
| 6 | `fuzz/fuzz_targets/srtp_use_ext.rs` | Delete the file. Also delete the corresponding `[[bin]]` block in `fuzz/Cargo.toml:98-103`. |
| 7 | `crates/rsiprtp/tests/baresip_integration/scenarios/security.rs` | Add a one-line comment at line 112 above `test_dtls_srtp_simulation`: `// NOTE: simulation only — rsiprtp does not implement DTLS-SRTP.` Test stays. |
| 8 | `CHANGELOG.md` | (a) Rename `## [Unreleased]` heading to `## [0.3.0] — 2026-05-02`. (b) Insert a fresh empty `## [Unreleased]` block above it. (c) Under `[0.3.0] / ### Removed` add: `**DTLS-SRTP stub** (\`rsiprtp::srtp::dtls\`). The module never contained a DTLS handshake — only fingerprint parsing, role enums, and a use-srtp-extension codec. SRTP key exchange is via SDES only. If DTLS-SRTP support arrives later it will be designed against an actual DTLS crate, not retrofitted onto these types.` (d) Update the link footer: `[Unreleased]: …compare/v0.3.0...HEAD` and add `[0.3.0]: …/releases/tag/v0.3.0`. |
| 9 | `Cargo.toml` (workspace root, line 15) | Bump `version = "0.2.0"` → `"0.3.0"`. |
| 10 | `crates/rsiprtp/public-api.txt` | Regenerate after build passes: `cargo install cargo-public-api --locked` (if not present), then `cargo public-api -p rsiprtp > crates/rsiprtp/public-api.txt`. Expect several hundred `Removed` lines under `pub mod rsiprtp::srtp::dtls`. |

## Out of scope

- Implementing real DTLS-SRTP (Option B in the brief). Future brief + HLD.
- Touching `srtp::sdes`, `srtp::context`, `srtp::kdf` — unrelated, working.
- Refactoring `security.rs` integration tests beyond the one comment.
- Dependency pruning. Verified: `sha1` (used by `srtp/context.rs`,
  `ice/agent.rs`, `ice/turn.rs`), `sha2` (used by `sip/auth.rs`),
  `thiserror` (used by 13 files) all retain non-DTLS callers.

## Public API impact

Removes module `rsiprtp::srtp::dtls` and the 11 items it re-exported via
`srtp/mod.rs` (`DtlsConfig`, `DtlsError`, `DtlsRole`, `DtlsSrtpKeys`,
`DtlsState`, `Fingerprint`, `FingerprintHash`, `SrtpProfile`,
`extract_srtp_keys`, `parse_use_srtp_extension`,
`build_use_srtp_extension`).
Surviving `rsiprtp::srtp` surface: `SrtpContext`, `SrtcpContext`,
`CryptoSuite`, `SessionKeys`, `SdesAttribute`.
No `Deprecated` step — dev sandbox, clean delete.

## Validation

```bash
cargo build --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo doc --no-deps -p rsiprtp                # catches any broken intra-doc links
cargo check --manifest-path fuzz/Cargo.toml   # standalone workspace, not covered above
```

Acceptance:

- All commands clean, no new warnings.
- Tests deleted with `dtls.rs` (~30 unit tests in its `mod tests`) — expected.
- `security.rs::test_dtls_srtp_simulation` still passes (never imported `srtp::dtls`).
- `public-api.txt` diff shows only `Removed` entries under
  `rsiprtp::srtp::dtls::*`; no `Changed` entries.
- Final grep for `dtls`/`Dtls`/`DTLS` returns only: the new CHANGELOG
  entry, the `security.rs` simulation comments (with the new NOTE),
  the brief and HLDs in `wrk_docs/`. Nothing in `crates/`, `fuzz/`,
  `README.md`, `ARCHITECTURE.md`, `Cargo.toml`.
